describe('games', () => {
  describe('start game', () => {
    describe('given validation check fails', () => {
      it('should return a 404', () => {
        expect(true).toBe(true);
      });
    });
  });
});
